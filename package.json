{
  "name": "Overwatch-Stat-Manager",
  "version": "1.0.0",
  "main": "index.js",
  "repository": {},
  "license": "MIT",
  "dependencies": {
    "concurrently": "^4.0.1"
  },
  "scripts": {
    "start": "concurrently \"cd webapp && npm run start\" \"cd dbapp && npm run start\"",
    "heroku-postbuild": "cd webapp && npm install && npm run build"
  }
}
