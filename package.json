{
  "name": "Overwatch-Stat-Manager",
  "version": "1.0.2",
  "main": "index.js",
  "repository": {},
  "license": "MIT",
  "cacheDirectories": ["webapp/node_modules", "dbapp/node_modules"],
  "dependencies": {
    "concurrently": "^4.0.1"
  },
  "scripts": {
    "start": "cd dbapp && ls && npm install && node index.js",
    "heroku-postbuild": "cd webapp && npm install && npm run build"
  }
}
